#!/bin/sh

[ -z "$3" ] &&
  echo "Usage: downloadsong <title> <artist> <link>" && exit 1
filename="$(echo "$1" | sed 's/ /-/g')"
youtube-dl -x -o "$filename.%(ext)s" --audio-format vorbis "$3"
vorbiscomment -t "TITLE=$1" -t "ARTIST=$2" -w "$filename.ogg"
