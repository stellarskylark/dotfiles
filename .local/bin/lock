#!/bin/sh
fort=$(fortune christian)
[ -e $(pactl list sinks | grep Port: | grep -o speaker) ] ||
  amixer -D pulse set Master 1+ mute && pkill -RTMIN+10 dwmblocks
slock -m "$(figlet -w $(echo "$fort" | wc -L) -c LOCKED)
$fort"
